CREATE TABLE Phrase (
  id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
  message TEXT NOT NULL
);

selectAll:
SELECT *
FROM Phrase;

select:
SELECT *
FROM Phrase
WHERE id = ?;

insert:
INSERT OR REPLACE INTO Phrase (
message
)
VALUES ?;
